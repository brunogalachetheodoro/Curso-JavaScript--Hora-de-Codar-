let nomes = ['matheus', 'maria', 'bruno']

for (let i = 0; i <= nomes.length; i++) {
    console.log(nomes[i])
}

let jogos = ['red dead', 'god of war', 'the last of us']
let cont = 0

while (cont <= jogos.length) {
    console.log(jogos[cont])
    cont++
}