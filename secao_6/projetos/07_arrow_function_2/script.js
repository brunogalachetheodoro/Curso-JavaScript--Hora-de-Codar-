//os dois códigos são para as mesmas coisas, porém, um é mais enxuto
const multp2 = (x) => {
    return x * 2
}
console.log(multp2(2))

// quando o código é simples pode ser feito em uma linha
const mult = x => x * 2;
console.log(mult(4))